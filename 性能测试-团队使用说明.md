# 性能测试平台 · 团队使用说明

> 面向**使用者**（下发任务 / 看报告 / 在自己机器采集）。
> 你**不需要**任何代码权限，也不用碰服务器。
>
> **平台地址：`http://<平台IP>:8000`**　←　由管理员填好后转发（例如 `http://10.18.130.34:8000`）
> 账号：向管理员申请（在「用户管理」开号）。

---

## 我该看哪一节？

| 你想做的 | 看 | 要装东西吗 |
|---|---|---|
| 看性能报告、下发采集任务 | **A** | 不用，只要浏览器 |
| 在**自己的被测机**上跑性能采集 | **B** | 要装 agent（一次性 5 分钟） |
| 测**冷启动 / 对话**等要手动操作的场景 | **C** | 同 B，采法不同 |

---

## A. 看报告 / 下发任务（零安装）

1. 浏览器打开 `http://<平台IP>:8000`，登录。
2. 左侧 **性能测试 → 性能报告**：顶部选「报告集」看对比曲线、KPI、结论；点「刷新」拿最新。
3. 左侧 **性能测试 → 任务下发**：
   - 先「新建报告集」（默认时间戳名，可重命名）——这次采集会归入它，报告里独立展示。
   - 场景选「长监控」、时长填 `40s`（或 `30m`/`12h`），对象标签填被测版本号。
   - 点「下发」。

> ⚠️ 下发的任务**需要一台执行机在线**才会真正跑（见 B）。如果你只是看报告，让有执行机的同事去采即可。
> 任务一直停在 `pending` = 目标执行机没开着。

---

## B. 在自己机器上采集（装 agent，一次性）

在**被测机器**（装了纳米 Work 的 Windows）上做：

### 1. 装 Node.js
到 https://nodejs.org 下载 **LTS 版**装好。命令行 `node -v` 能出版本号即可。

### 2. 下载 agent
平台左侧 **测试工具广场 → 工具广场** → 卡片「**性能测试执行机 agent**」→ 点「**可下载**」→ 得到 `perf-agent.zip`，解压到任意目录（如 `D:\perf-agent`）。
（包里有 `DEPLOY.md`，更详细的步骤在里面。）

### 3. 注册执行机、拿 token
平台左侧 **我的工作台 → 我的设备** → 「注册设备」：
- runner_id：给这台机起个标识，如 `win-01`
- 名称：如「张三的测试机」

注册后会返回一串 **token（只显示这一次，复制好）**。

### 4. 配置
解压目录里，把 `.env.example` 复制改名为 `.env`，用记事本填 3 项：
```
BASE_URL=http://<平台IP>:8000        # 注意是 8000（平台后端），向管理员确认 IP
RUNNER_TOKEN=<第 3 步复制的 token>
RUNNER_ID=win-01                     # 和注册时填的一致
```

### 5. 启动
双击 **`run.cmd`**。看到 `perf-agent 轮询启动 · runner=win-01` 就成功了，**保持这个窗口开着**。

### 6. 采集
回平台 **性能测试 → 任务下发** → 选**自己的执行机** → 下发长监控 → 采集完成后到 **性能报告** 刷新查看。

---

## C. 交互场景采集（冷启动 / 对话 / 热启动等）

这些场景要人工操作应用，不能无人值守。装好 agent 后，在被测机上：

1. 双击解压目录里的 `纳米性能测试.bat`，按菜单选场景（如 `[1]对话`、`[3]冷启动`）。
2. 按屏幕提示操作：先采基线别动 → 回车开始 → **真实操作纳米 Work** → 回车结束。
3. 采完在解压目录运行：`node perf-agent.mjs upload`
4. 回平台 **性能报告** 刷新即可看到。

---

## 采到"真实数据"的前提（重要）

- **被测应用（纳米 Work）必须在这台机器上运行**，且采集期间**真实操作它**（切会话、发消息）——否则 CPU/内存是平线（应用空转，不是故障）。
- 想要 **FPS / 启动分段耗时**：先彻底退出纳米 Work（含右下角托盘），再带调试端口启动：
  ```
  & "安装路径\Namiwork.exe" --remote-debugging-port=9222
  ```
  不带也能测，只是没有 FPS。
- 测**竞品**：下发时填「竞品进程」（如 `Doubao.exe`），只采系统级指标。

---

## 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 任务一直 `pending` | 执行机的 `run.cmd` 没开着；或 `RUNNER_ID` 和下发时选的执行机对不上 |
| 报告里 CPU/内存全是 0 | 被测应用没在跑；或采集时没操作应用（空转） |
| FPS 一直空 | 没带 `--remote-debugging-port=9222` 启动应用 |
| 提示「无效的 runner 令牌」 | `.env` 的 `RUNNER_TOKEN` 填错，或设备被删——到「我的设备」重拿 |
| 打不开平台页面 | 确认用的是管理员给的 `http://<平台IP>:8000`（不是 localhost）；确认在同一内网 |

---

*有问题找平台管理员。*
