# 回归用例导出 Playwright 脚本 — 开发本地运行说明

> 面向:拿到测试同学导出的 `.spec.mjs` / `.zip`,想在本地跑起来自测的**开发**。
> 这些脚本由测试管理平台「回归用例库」导出,**自包含**——每个 `.spec.mjs` 顶部都写了运行前置,本文档是同一份说明的展开版。

---

## 一、脚本怎么来(测试同学操作,开发略读)

测试同学在平台 **回归用例库** 页面:
- **单条**:某行「导出脚本」→ 得到 `case-<id>.spec.mjs`。
- **批量**:勾选多条 → 顶部「导出脚本」→ 得到 `playwright-cases.zip`(每条一个 `.spec.mjs`)。
- 只有 **GUI/E2E 且已生成 script** 的用例可导;API/纯人工/无 script 的用例不支持(批量会自动跳过并提示)。

拿到 `.mjs` 或解压 `.zip` 后的一堆 `.mjs`,按下面步骤跑。

## 二、一次性环境准备

在放脚本的目录里:

```bash
# 1) 若目录还没有 package.json
npm init -y

# 2) 装 Playwright 测试运行器
npm i -D @playwright/test

# 3) 首次装浏览器内核(用于 CDP 连接,装 chromium 即可)
npx playwright install chromium
```

> Node 建议 18+。以上每台开发机只需做一次。

## 三、跑之前:把被测客户端带调试端口启动(关键)

脚本**不自己开浏览器**,而是通过 **CDP 连上你本机已经跑着的被测客户端**(Electron),驱动它内嵌的业务页。所以每次跑之前:

1. **带远程调试端口启动客户端**:
   ```bash
   <客户端可执行文件> --remote-debugging-port=9222
   ```
   - macOS 举例(按实际路径改):
     ```bash
     "/Applications/纳米Work.app/Contents/MacOS/纳米Work" --remote-debugging-port=9222
     ```
   - Windows 举例:
     ```bat
     "C:\Program Files\纳米Work\纳米Work.exe" --remote-debugging-port=9222
     ```
2. **先登录、并停在用例涉及的页面**。脚本从"客户端当前状态"接着往下跑,**不含登录流程**(除非该用例本身就是登录 E2E)。
3. 保持客户端开着(脚本默认连 `http://127.0.0.1:9222`;业务页在内嵌 iframe `iframe[src*=".work.n.cn"]` 里,脚本已自动处理这层作用域)。

## 四、运行

```bash
# 单个
npx playwright test case-123.spec.mjs

# 或:zip 解压后,跑目录下全部
npx playwright test

# 想看着浏览器跑(调试用)
npx playwright test case-123.spec.mjs --headed
```

## 五、脚本里两类需要你手动补的地方

导出脚本尽量翻译了每一步,但有两类会显式标出来,**跑之前先处理**:

1. **`// TODO: …无通用 Playwright 对应,请手写`**
   多为"发消息后等 AI 回复生成完成(`wait_response`)""主观判定(`judge`)"这类没有通用 Playwright 写法的步骤——按注释里的判定点自己补断言。

2. **`throw new Error('用例导出:该步选择器待补,见上方 TODO 注释')`**
   这一步用到的元素**还没在平台登记选择器(缺 key)**,脚本无法定位,故意抛错防止静默跳过。
   → 正确做法:让测试同学在平台「选择器管理」把该 key 补齐(可用用例库的「**定位缺失 key**」一键在页面上找到对应元素并登记),**补齐后重新导出**,新脚本这一步就完整了。

> 脚本里一个语义定位常用 `.or()` 串了多个候选并以 `.first()` 收尾(自愈 + 避免多命中报错),这是正常的,不用改。

## 六、常见问题

- **连不上 `127.0.0.1:9222` / connectOverCDP 报错**
  客户端没带 `--remote-debugging-port=9222` 启动,或没先跑起来。**注意**:打包后的正式客户端可能因 Electron fuses 关掉了远程调试而连不上——若确认已加参数仍连不上,需要用**允许远程调试的构建**。
- **定位不到元素 / 超时**
  多半是没停在用例涉及的页面,或登录态没就位;也可能是该 key 的选择器在平台里过期了(让测试同学在「选择器管理」重新探测校准)。
- **端口被占用**
  换个端口启动客户端(如 `--remote-debugging-port=9333`),并把脚本里 `connectOverCDP('http://127.0.0.1:9222')` 的端口同步改掉。

---

如脚本本身翻译有误(不是上面两类待补),请把 `.spec.mjs` 连同用例反馈给测试/平台维护同学。
