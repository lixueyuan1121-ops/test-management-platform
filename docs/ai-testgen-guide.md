# AI 测试助手 · 生成与排障说明

> 面向 QA:用「AI 测试助手」从需求生成结构化测试点时,如何拿到更高的自动化执行率、以及遇到问题怎么办。
> api 类用例的配置/执行另见 [`api-testcase-user-guide.md`](./api-testcase-user-guide.md)。

## 用例类型与自动化

生成的每条用例有一个**执行类型 kind**,决定它能否被执行机自动跑:

| kind | 含义 | 能否自动执行 |
|---|---|---|
| **gui** | 客户端界面上单点操作+断言(含进入/恢复通常 3–6 步) | ✅ 走 step-executor |
| **e2e** | 跨界面的端到端流程(≥5 步) | ✅ 走 step-executor |
| **api** | 脱离客户端直接调接口 | ✅ 走 api-executor(**仅当接口能在客户端外鉴权**,见 api 指南「适用边界」) |
| cli | 命令行 | ✅ 走 claude |
| **manual** | 人工执行 | ❌ 不下发,占人力 |

**目标:manual 越少越好。** 生成侧已内置**「自动化优先」**倾向:

- 只要验证点能"在界面上操作并做**客观断言**",一律优先判 **gui/e2e**,不轻易判 manual;
- 后端/接口行为若在界面有可观察结果(创建后列表出现、删除后消失、报错弹提示),**优先设计成 gui/e2e**(界面触发+断言),而非 api;
- 只有**依赖人主观判断**的(美观、流畅、体验)才判 manual;
- 项目**没配 api 契约**时,本会判 api 的验证点会**自动改判 gui/e2e**(因为这类系统接口多半客户端外调不通)。

> 前提:gui/e2e 能落地,靠项目里有对应的**选择器 key**(在「选择器管理」用探测积累)。key 越全,能自动化的用例越多。

**用例自治(连续执行不打架)**:执行机会在同一客户端连续跑多条 gui/e2e 用例、用例间不重置页面。故生成规则要求每条用例**从初始态开始**(connect 后先用导航 key 显式进入目标页、默认已登录主界面、登录单列为一条 e2e)、并在**结尾恢复 UI 瞬态**(关本用例开的弹窗、清填写的输入、必要时回起点页)。缺导航/关闭 key 时会走「选择器待补」,用「定位缺失 key」桥接补齐后即恢复自动化。

## 用例与「选择器页面」挂钩 —— 帮生成、帮维护

选择器管理里每个 key 可归属一个**页面**(如"登录页/任务页")。用例可以和这些页面挂钩,带来两块价值:

**① 帮生成(更聚焦、少降级)**:AI 生成表单有「**目标页面**」多选(候选来自该项目选择器里已有的页面)。选中后:
- 生成时**只把该页面(+未分类)的 key 注入 prompt**——噪声小、模型更容易挑到合适 key,降级成 manual 的更少;
- 生成出的这批用例自动标上该页面。
- 不选页面 = 注入全部 key(与以前一致),跨页 e2e 可多选页面。

**② 帮维护(按页定位、批量更新)**:
- 每条用例的页面**优先按它 script 里用到的 key 自动推断**(用了 `navTasks`→任务页);无 key 的 manual/api 用例回落到生成时所选页面;也可在用例库**编辑**里手动改。
- 用例库有**「页面」列** + 顶部**页面筛选**。当某个页面的选择器改版时,按该页面筛出全部相关用例 → 配合「仅待补选择器 / 批量一键重生」批量更新。
- 「保存并重生 script」后,页面会**按新 script 用到的 key 重新校正**(script 变了,归属页面随之更新)。

## 回归用例库 —— 独立入口,按页面勾选直接执行

导航「用例设计」下新增**「回归用例库」**(与「用例库」「已采纳用例」同级)。把长期要复用的用例纳入回归库,即可在此**按页面随选随跑**,不必先采纳、也不必挂到某个任务上。

- **标记回归**(在「用例库」做):勾选若干用例 → 顶部「**标记回归**」/「取消回归」;或在用例**编辑**里打开「回归」开关。用例行有「回归」列一眼可见。
- **回归用例库页**(执行入口):默认只列回归用例。顶部选**页面**筛出该页回归用例 → 勾选(可「全选本页」)→ 选执行设备 → 点「**执行回归**」。
  - 与「发送到执行机」的区别:执行回归**不要求已采纳、不要求关联任务**,随选随跑;只会**跳过 manual**(不可自动化)用例。
  - 执行结果照常在**「执行结果」页**查看(回归执行不回流验收清单,不影响任务清单口径)。

## 执行结果:按批次汇总 + 逐步截图报告

「执行结果」页把每次下发聚成一个**批次**展示:

- **按批次分组**:每次「发送到执行机 / 执行回归」是一个批次(带批次号)。组头一眼看到 **总数 / 通过 / 失败 / 通过率 / 总耗时 / 设备 / 时间**;展开看该批每条用例。
- **单条报告下钻**:gui/e2e 用例点「**报告**」→ 看这一条这次执行的**逐步清单**(每步动作/说明/✓✗/失败原因),关键步与失败现场附**截图**(点击放大)。
- 截图策略:关键断言步通过后、以及任一步失败时自动截图;执行机上传到平台,报告里内嵌可看(不再是"仅本地路径")。
- 老记录(无批次/无报告)自动回落到原「证据(本地路径)」提示,向后兼容。

> 范围:逐步截图报告目前覆盖 gui/e2e(结构化执行);api 用例进批次汇总(算通过/失败),详情暂为原因摘要。

## 「补选择器可自动化」标识 —— 把降级用例捞回来

有些用例本该是 gui/e2e,但**因为用到的选择器 key 还没注册**,生成时被降级成了 manual。平台会给这类用例打一个醒目标识,让你知道"**差一点点就能自动化**",并**直接列出缺哪些 key**(无需 hover):

- **AI 测试助手 · 生成结果表**:测试点下方直接显示橙标签「补选择器可自动化 · 补: navTasks, submitBtn」——生成完当场就看到;
- **用例库 · 执行类型列**:同样的标签,并在标签内换行列出缺的 key;
- **用例库 · 顶部筛选**:勾选「**仅待补选择器**」可一键筛出全部这类用例,勾选后旁边显示条数(如"仅待补选择器 · 12"),便于批量处理;
- **用例详情**:「判定理由」一行显示完整文案。

该标识**可信**:只有"补齐这些 key 后确实能通过校验"时才承诺"补齐即可执行";若还有其它问题(如缺断言),会注明"补齐后仍需修其它问题"。

**闭环操作(把 manual 转成自动化):**

1. 用例库勾「仅待补选择器」筛出全部这类用例 → 看标签里列的缺失 key(无需 hover);
2. 去「选择器管理」,用**设备探测**把这些 key 补进注册表;
3. 回到该用例:**编辑 → 「保存并重生 script」**。后端**一键按原意图(gui/e2e)重生**——补齐 key 后即生成合法 script、**自动把执行类型从 manual 恢复为 gui/e2e 并清除标签**,无需手动改类型。

> **批量**:用例库勾「仅待补选择器」后,顶部出现「一键重生全部」——补齐 key 后一次把这批降级用例逐条转回 gui/e2e(调 AI 较慢、带进度,仍缺 key 的会跳过并保留标签,补齐后再点一次即可)。
> 若 key 还没补全,重生会报错(生成的 script 仍缺 key),标签保留提醒你继续补。如此逐批把"降级人工"的用例转成自动化,持续减轻人力。

> **重生失败怎么回看**:批量重生结束若有失败,会弹**「重生失败清单」**逐条列出用例 + 具体原因(如"用了未注册的 key「navTasks」");单条重生的失败原因也会**落库**,在**用例详情**的「上次重生失败」一栏随时回看。照原因补齐对应 key 再重生即可(重生成功该记录自动清除)。

## 生成的稳健性(平台已做的保护)

- **解析容错**:模型输出被网关截断、前后夹了多余文字、或个别用例格式坏时,平台会**逐条抠出能用的用例**(而不是整批丢弃)。所以"生成成功却一条没出"的情况已大幅减少。
- **script 非法自动降级**:gui/e2e 若凑不出合法断言/注册 key,自动降级 manual(并可能带上「补选择器」标识),**不会把坏脚本派到执行机**。
- **重生 script 超时放宽到 60s**:单条「保存并重生 script」调 AI 较慢,前端已单独放宽超时,不会因为默认 15s 提前报错。

## 常见现象

| 现象 | 说明 / 处理 |
|---|---|
| 生成完提示「未检测到有效测试点」 | 模型没产出可解析的用例数组。看该次任务的 `output_raw`(错误信息带尾部片段);多为需求太空泛或模型跑偏,**换更具体的需求重试**。 |
| 大量用例是 manual | 该项目选择器 key 太少,或需求偏主观。先在「选择器管理」补 key,再看带「补选择器可自动化」标签的用例批量转化。 |
| 想要更多 gui/e2e、更少 api/manual | 已是默认倾向。若仍偏多 manual,通常是"没有可断言的界面元素/key"——补 key 是根本解。 |
| api 用例全 fail(110001/4001) | 被测系统客户端内签名,api 客户端外调不通 → 改判 gui/e2e(见 api 指南「适用边界」)。 |
