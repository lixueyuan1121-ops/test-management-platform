# 平台 UI 与交互改版

## 目标与边界

统一导航、页面层级、筛选与批量操作、详情阅读和确认流程。保留现有 URL、权限判断、接口、任务调度与判定语义。不同模块分批验收，不通过全局覆盖样式强行改造所有页面。

## 收尾验收

本轮本地实现与验收完成：26 组 `frontend/tests/*-ui.cjs` 串行回归全部通过，全部视图/布局模板编译及 `git diff --check` 通过。源码范围、关键写操作参数、权限入口和桌面/手机布局的对应证据见下方索引。通过 `scripts/release.sh main` 构建并推送交付；服务器的 `scripts/update.sh` 生效确认仍需单独完成，不以代码推送冒充线上部署。

下方“未发布”“待继续”等为实施过程记录，不是最终验收状态；本节及后续发布记录优先。

## 导航结构

- 工作空间：工作台、测试指挥官、我的日报。
- 对话测评：测评任务、用例库、测评生成、测评结果。
- 功能测试：测试计划、任务分配、AI 测试助手、用例资产、执行结果与遗留问题。
- 反馈回归：导入记录、反馈用例、回归集、回归结果。
- 性能测试：任务下发、性能报告。
- 质量与发布：作战大屏、需求覆盖、上线 checklist、发版记录、统计与质量分析。
- 设备与工具：设备看板、我的设备、工具广场。
- 系统管理：项目、用户、选择器、API 环境、工具配置；只对管理员显示。

默认展开当前模块；侧栏功能搜索仅搜索当前身份有权访问的入口。保留折叠状态，路由切换后显示所属模块和当前页面，并重置主内容区滚动。

窄屏（不超过 700px）默认收起侧栏，导航到新页面后自动收起；桌面折叠偏好独立持久化，不受手机临时展开操作影响。跨越断点时恢复对应状态。

## 页面规则

1. 列表页采用页标题 + 操作栏 + 数据列表，不以大卡片包整页。主操作突出，低频操作收进菜单，停止与删除保留确认。
2. 搜索、筛选、已选数量、提交操作需要能在长列表中访问；窄屏可以折叠配置，不能让固定区域占满屏幕。
3. 编辑表单使用受视口约束的滚动内容区，保存与取消留在固定底部。选择资产时提供“全部 / 已选”视图，跨筛选保留选择。
4. 详情分为结果、报告等标签，不在长表格末尾堆报告。标签与批次选择吸顶，行动按钮随当前标签出现。
5. 长提问、预期和原始证据在侧栏阅读；列表只显示摘要。缺记录与失败明确区分，不通过 UI 捏造成功状态。
6. 相同配色与尺寸：石墨侧栏、白色工作区、钴蓝主操作；红/绿/琥珀仅表达状态；间距以 4/8px 为基准，固定工具面板圆角不超过 8px。

## 实施顺序

本轮：全局导航 + 测评任务列表/详情/编辑 + 测评用例库的阅读与下发确认；保留已完成的结果侧栏与吸顶操作栏。

后续：功能测试全链路、反馈回归与性能模块、设备和质量看板、低频管理页。每一批复用上述规则，单独补关键流程测试；不把未验收模块标记为完成。

## 当前进展

本轮发布范围：下文标记为“未发布”的生成页、登录/作战大屏、AI 战绩墙、性能报告、反馈与管理表单补充修复统一纳入本次 release.sh；这些标记保留为实施过程记录。质量聚焦/回归智选的写操作、设备异常态和剩余管理流程仍继续验收，不在本次完成声明内。服务器生效须另行执行 update.sh。

- 本轮已推送 main：8f6464e2（源码与七组新增测试）、e5ad44e8（构建产物）。发布前十二组 UI 回归及全部页面模板检查通过；release.sh 构建、推送成功，服务器更新未执行。
- 发布后继续质量模块：回归智选增加下发确认、加载失败重试和版本请求隔离；质量聚焦按勾选需求的失败数控制分析、固定批量操作、建草稿防重复、长需求换行。rts-ui / fail-clusters-ui 验证原 API 参数、取消不下发、失败重试、迟到旧版本不覆盖新版本、手机布局。保留风险评分/默认高分勾选/聚类算法不变。这部分尚未提交发布；设备异常态、剩余管理及发版写操作仍待验收。
- 设备补验：设备看板首次失败显示未获取数据而非零指标/无设备，后续失败保留历史数据与时间，静默轮询避免每五秒弹错；我的设备补错误重试、保存锁定、重置/删除防重复和中文确认。device-errors-ui 验证首次失败、历史时间戳、离页轮询清理、编辑失败保留输入、runner_id 不可修改、重置/删除取消与确认参数；admin-devices-ui 原有注册/手机/管理回归和全部页面模板检查通过。不更改后端设备归属、队列或统计计算规则。本批未提交发布，发版/清单写操作和其余管理流程继续验收。
- 上线清单补验：release-checklist-ui 验证执行确认/取消、manual 跳过和全人工禁止下发、关联版本与原队列参数、批量移出确认及失败保留勾选、项目切换清空旧选择、读取重试和迟到响应隔离。发版写操作 release-writes-ui 验证创建项目归属、编辑渠道/正文/备忘参数、保存失败保留输入、删除取消/失败重试、成员只读入口；表单增加保存中锁定。admin-devices-ui 原有质量图表回归及全部页面模板检查通过。本批未发布；发版列表/详情/统计异常态和剩余工具管理操作仍待继续，不以写操作测试代替全页验收。
- 发版读取补验：release-reads-ui 验证列表、概览、质量档案、详情分别失败与重试，详情失败不再冒充完整内容，旧项目响应不覆盖新列表；统计失败不阻断列表。增加请求顺序和卸载保护，概览失败隐藏旧指标。release-writes-ui、admin-devices-ui（含桌面/手机图表）及全部页面模板回归通过。本批未发布。接下来继续工具配置、API 环境、选择器完整管理流程与全局收尾审计。
- 工具配置补验：tool-admin-ui 验证分类/工具加载失败独立重试、编辑失败保留内容及字段参数、创建工具、上下线、工具与分类删除确认/取消；分类删除继续明确提示连带删除工具，删除当前筛选分类后清除失效筛选。增加请求顺序保护、提交锁定、取消/网络异常处理。admin-devices-ui 原有分类创建及全部页面模板检查通过。本批未发布；API 环境、选择器管理和全局收尾仍待继续。
- API 环境补验：api-env-ui 验证读取/保存失败重试、保存字段参数、项目切换清除旧配置及迟到响应隔离、重置取消不丢编辑；cURL 解析失败保留原文、修改原文使旧结果失效、并入契约和 base_url 补充；OpenAPI 失败重试、替换/追加、原文失效及项目参数。手机解析弹窗和底部按钮边界通过并截图检查。保存/解析期间锁定输入和关闭操作，不改变后端鉴权与解析规则。admin-devices-ui 原保存回归及全部模板检查通过。本批未发布，选择器和全局一致性审计继续。
- 选择器状态补验：selector-states-ui 验证注册表/自学习候选独立读取失败重试、转正/拒绝参数和取消不提交、编辑失败保留内容、连续探测读取失败仍在 60 秒超时。列表增加请求顺序隔离，切换作用域清除旧探测结果；编辑/评审/探测期间锁定项目作用域，卸载后迟到探测响应不重启轮询。admin-devices-ui 新增 key 和缺失 key 深链接自动探测回归、全部页面模板检查通过。本批未发布；删除影响提示、内置导入、探测结果加为 key 以及全局一致性仍需补验。
- 选择器动作补验：selector-actions-ui 验证引用范围读取失败暂停删除、引用用例/降级提示、删除与导入取消不发请求、内置导入项目参数、探测元素新建 key 和更新已有候选参数。修正表格“加为 key”未使用 frame 兜底的问题，保存采用已有 _frameMatch；候选排序/上限和回填规则不改。selector-states-ui、admin-devices-ui 和全部模板检查通过。本批未发布，进入全局导航/移动端/长页面操作区及全套回归收尾，不代表全量审计已完成。
- 全局导航收尾：navigation-responsive-ui 验证手机初始收起、路由切换后收起、桌面偏好独立保存、分组搜索与无匹配状态、断点往返。既有测试改为等待自动折叠状态，移除会与响应式更新竞争的额外点击，不放宽业务/布局断言。当前 23 组 UI 回归全部通过，全部视图与布局模板检查及 diff 空白检查通过；本批未提交发布。最终仍需按范围核对覆盖与代码审查后整理发布，模拟回归不替代服务器更新确认。
- 范围核对补缺：需求覆盖的挂用例选择器原先搜索会丢选择，现按 id 保留跨筛选选择并增加“候选用例/已选”视图；读取失败清空旧候选并保留已选，禁用误提交。需求用例读取独立重试，摘除增加确认，保存中锁定表单。requirements-actions-ui 验证编辑解除发版仍传 0、已挂用例排除、跨搜索两项同批挂接、已选视图、候选失败重试、摘除取消/确认参数。admin-devices-ui 原需求创建和全部页面模板检查通过。本批未发布，最终范围核对与差异审查继续。
- 统计与遗留问题补验：statistics-states-ui 验证日报/工作量首次失败不显示零统计或“全部已交”、失败重试、已交/未交及非空图表、刷新失败隐藏旧图、手机边界；增加请求顺序/卸载保护。issues-ui 验证读取重试、关联缺陷失败保留输入、标记解决/重开参数、极库云部分同步失败提示和上报取消；外部链接仅将 http(s) 渲染为可点击。统计计算、状态枚举和极库云后端联动不变。两组及 admin-devices-ui 回归通过，当前全套共 26 组，发布前全量重跑中。

## 验收证据索引

| 范围 | 主要证据（frontend/tests） |
| --- | --- |
| 导航、权限、桌面/手机断点 | navigation-responsive-ui、eval-workspace-ui、admin-permissions-ui |
| 测评任务、库、结果、分析与推送选择 | eval-workspace-ui、eval-results-ui |
| 两类生成器与取消/历史/回写 | generation-ui |
| 功能测试与性能下发/采集 | functional-workspace-ui |
| 反馈导入、用例、回归集、结果 | feedback-actions-ui、feedback-results-import-ui、functional-workspace-ui |
| 性能报告及阈值 | performance-report-ui |
| 设备、管理账号/项目/成员、工具广场 | admin-devices-ui、admin-permissions-ui、device-errors-ui |
| 需求、上线清单、发版读写 | requirements-actions-ui、release-checklist-ui、release-reads-ui、release-writes-ui |
| 日报/工作量、战绩墙、作战大屏 | statistics-states-ui、ai-wall-ui、login-war-room-ui |
| 回归推荐与失败聚类 | rts-ui、fail-clusters-ui |
| 选择器、API 环境、工具配置 | selector-states-ui、selector-actions-ui、api-env-ui、tool-admin-ui |
| 工作台、指挥官、我的日报、登录 | workspace-ui、login-war-room-ui |
| 遗留问题 | issues-ui |

上述为模拟接口的前端流程、请求参数及布局证据；不声称覆盖外部平台在线行为或服务器部署。当前收尾批次未修改 backend、runner、路由表或鉴权 store；服务器更新确认单独记录。

- 第一批：导航与测评模块已随 d83eacf4 / d4dc4304 推送 main；服务器需运行 update.sh 生效。
- 第二批：测试计划、任务分配、功能用例库接入统一页面框架，新增计划/任务筛选、窄屏筛选收起、吸顶批量操作和自适应弹窗。已随 6eb4c2c8 / 349268a6 推送，完整验收范围见下文。
- 第三批：已采纳/回归用例增加全文侧栏和吸顶选择区；执行结果接入统一框架，未全部判定的批次不再显示全部通过；反馈四个页面采用统一布局，反馈详情保存固定在抽屉底部。
- 反馈写操作补验：feedback-actions-ui 验证详情五字段保存、失败保留编辑、手机固定底部、人工用例禁选、按已注册设备下发、无设备禁止下发、加入回归集、删除取消、立即回归确认/取消以及 cron/enabled 参数。移除反馈用例库写死的 mac-01 默认设备，确认按钮文案统一中文；补充修正未发布。
- 反馈导入/结果补验：feedback-results-import-ui 验证列表和详情加载失败重试、展开完整原因、安全证据链接、手机长批次号和正文、上传解析失败保留文件及 multipart 重试参数。新增明确错误态和详情请求顺序保护；原有 functional-workspace 回归通过。本批未发布，最终全量回归和权限审计继续。
- 第四批：性能下发配置改为弹窗，性能报告和采集控制接入统一页头。functional-workspace 验证下发参数、报告空态和采集推进；performance-report-ui 补验有数据图表非空、指标切换、阈值 max/min 参数、保存失败保留输入、手机弹窗内容滚动与固定底部、加载失败重试、旧请求晚返回不覆盖当前报告。报告加载新增错误态与请求顺序保护，阈值表单改为响应式布局；这些补充修复尚未发布。
- 第五批进行中：设备看板增加设备/负责人/在线筛选（总指标仍为全平台口径）；我的设备、项目、用户、成员管理接入共用框架；工具配置拆分列表/分类标签；API 环境保存与重置移至吸顶操作区。已验证设备筛选和总数、创建参数、成员编辑权限与手机注册弹窗。
  工具分类创建与 API 环境保存参数已验证；修复开发代理误将 /api-env 页面匹配为 API 请求的问题。尚待设备异常态、完整管理操作和其余页面终验。
- 管理权限补验：admin-permissions-ui 验证七个管理员路由的普通用户直达拦截、成员/嘉宾隐藏管理操作、项目管理员改角色、移除取消不发请求、项目/用户/成员保存失败保留输入、重置密码失败重试和禁用参数。修复三页 finally 中无条件关闭弹窗，以及取消移除成员的未处理异常；不改变既有权限边界。admin-devices 原有回归通过。以上为前端权限与请求验证，最终全量验收仍待完成；补充修复未发布。
- 第六批进行中：遗留问题、需求覆盖、上线清单、日报/工作量统计采用统一布局；发版记录分为默认版本列表与质量概览，图表打开时初始化并响应尺寸变化。需求创建、发版图表桌面/手机非空渲染与返回列表已验证；发版写操作和清单执行仍需终验。
- 第七批进行中：选择器项目/作用域移至顶部，注册表/设备探测/候选评审分标签；保留缺失 key 深链接自动探测。工具广场增加搜索和键盘打开详情。新增 key 与深链接参数已验证。回归智选、质量聚焦已接入共用页头，核心推荐/聚类写操作待终验。
- 第八批：工作台精简页头，我的日报采用统一操作区，指挥官输入区固定并区分已取消/已执行状态；日报提交参数、指挥官二次确认与取消不执行、手机布局已验证。
- 2026-09-10 发布验收：五组 UI 脚本（eval-workspace、eval-results、functional-workspace、admin-devices、workspace）及全部页面模板编译通过。本批按 release.sh 推送；这不代表下述全量验收已完成，线上生效仍须服务器更新。
- 本批已推送：6eb4c2c8（源码）、349268a6（构建产物）。发布后继续 AI 战绩墙：统一吸顶筛选、缩小指标字号、修正漏斗宽度及手机排列；区分加载失败/空数据，漏斗失败时不再沿用旧数据，快速切换范围只接受最后一次请求结果。这部分属于下一批，尚未发布。
- 第九批：测评生成、AI 测试助手拆分配置/结果视图，保留输入和勾选，切换后重置滚动；结果摘要配全文抽屉。测评生成通过确认弹窗选择执行机和目标设备；测试助手历史入口在首次进入时即可使用，抽屉底部可评审。generation-ui 已验证两种生成请求、关联任务/场景组合参数、视图切换、勾选 ID、弹窗取消与确认下发、历史读取及采纳回写。补验历史加载失败重试、评审失败回滚、链接提取失败后保留正文、文件提取成功清除旧 URL、生成失败、停止等待忽略迟到结果、取消接口 200/409/500 三类提示及生成时锁定项目。权限与最终全量回归仍待完成；本批未发布。
- 第十批：登录页统一为品牌页头和单列表单，适配短屏并防止回车重复提交；作战大屏统一布局，按阶段名称读取漏斗统计、按数据源显示更新时间，首次失败与真实空数据分开，部分失败保留历史数据并提示，后台轮询不反复弹错。login-war-room-ui 验证必填校验、请求参数、防重复、失败重试、首次/部分失败、时间戳不冒充更新、恢复加载、30 秒轮询与侧栏切换后的定时器清理。桌面/手机截图和全部页面模板检查通过，AI 战绩墙回归通过；本批未发布。
- 待继续：设备与质量看板、管理页，以及生成/工作台等入口的整体一致性审计；各页的错误态、权限和剩余写操作需要最终全量回归。

## 全量验收范围

下列页面均需审计；已改动不等于验收通过，最终以各流程测试和桌面/手机截图为准。

- 对话测评：EvalTasks、EvalLibrary、EvalResults、AIEvalGen。
- 功能测试：TestPlans、Tasks、CaseLibrary、AdoptedCases、RegressionCases、ExecResults、AITestGen、Issues。
- 反馈回归：FeedbackImports、FeedbackCases、FeedbackRegression、FeedbackResults。
- 性能测试：PerfDispatch、PerfReport、PerfCollect。
- 设备与工具：DeviceBoard、MyDevices、ToolPlaza。
- 质量与发布：WarRoom、Requirements、ReleaseChecklist、ReleaseNotes、DailyStats、WorkloadStats、AIWall、FailClusters、RtsSelect。
- 系统管理：Projects、Members、Users、SelectorAdmin、ApiEnvAdmin、ToolAdmin。
- 工作空间与全局：Dashboard、Commander、MyReports、Login、MainLayout、导航权限及路由深链接。

## 验收清单

- 桌面和手机宽度、长文本、空数据、加载与错误状态。
- 深链接自动展开正确导航；菜单搜索和管理员/成员入口一致。
- 新建/编辑的字段、用例 ID、多产品设置与原接口一致。
- 执行/停止/重跑/定时/删除继续使用原接口与确认机制。
- 批次切换、结果与综合评价切换不丢数据、不重复触发写操作。
- 用例下发配置与变体/导入入口可用，不因筛选误下发。
- 模拟接口的 Playwright 回归、桌面/手机截图与原结果页回归；上线仍走 release.sh，由服务器更新后生效。
