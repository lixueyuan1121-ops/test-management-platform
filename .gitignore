node_modules/
.venv/
__pycache__/
*.pyc

# 前端构建产物（本地/CI 产物，不入库）。线上前端自行构建：
#   · docker：frontend 容器 Dockerfile 内 npm run build
#   · 裸机（如 :4173 vite preview / 有 Node 的机器）：开发机 npm run build 后本地起
# 若改用「服务器无 Node、纯 uvicorn 同源托管 dist」的部署，需另行在服务器构建或恢复入库。
frontend/dist/
.vite/

# Python 打包 / 测试 / 类型检查缓存
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 测试运行产物（Playwright 等）
playwright-report/
test-results/

.env
backend/.env
*.db
.vscode/
.idea/
.DS_Store
*.log
# 运行时上传（探测截图等）：不入库，仅服务器本地
backend/uploads/

# Claude 本地个人记忆（含推送习惯，勿入库）
CLAUDE.local.md
.claude/
